# 🗝️ Security Keys
SECRET_KEY=paste-your-super-secret-random-key-here

# 📊 Database Configuration

# For Local Development (SQLite — no setup needed):
# Leave DATABASE_URL blank and SQLite will be used automatically.

# For Production — MariaDB / MySQL (e.g., Railway, PlanetScale, Aiven):
# DATABASE_URL=mariadb://user:password@hostname:3306/dbname
# Note: The app automatically converts this to mysql+pymysql:// for SQLAlchemy.

# For Production — PostgreSQL (legacy, e.g., old Render setup):
# DATABASE_URL=postgresql://user:password@hostname:5432/dbname

# 🌐 Environment
FLASK_ENV=production
PORT=5000
